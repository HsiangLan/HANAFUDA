
// This component has been disabled as AI features have been rolled back.
// It is no longer used in the application.

// @ts-nocheck to suppress errors if it was imported elsewhere temporarily
export function HandAnalyzerForm() {
  return null;
}
